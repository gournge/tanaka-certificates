[project]
name = "tanaka-certificates"
version = "0.1.0"
description = "Numerical experiments for multidimensional Tanaka certificates"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "auto-lirpa",
    "matplotlib>=3.10",
    "numpy>=2.0",
    "scipy>=1.17.1",
    "torch>=2.11,<2.12",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-cov>=7.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tanaka_certificates"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: opt-in tests that run the production LP and formal verifier",
]

[tool.uv.sources]
auto-lirpa = { git = "https://github.com/Verified-Intelligence/auto_LiRPA.git" }
